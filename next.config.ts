// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Tells Next.js to produce static HTML files in /out after build
  output: 'export',
  
  // Add any other config you need here
  // e.g. images: { unoptimized: true }, etc.
}

module.exports = nextConfig
